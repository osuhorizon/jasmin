<div class="card">
    <div class="card-header">
        <% restricted = !(user.privileges & permissions.UserPublic) %>
        <%= restricted ? "Unrestict" : "Restrict" %> <%= user.username %> 
        <% if(user.ban_datetime > 0){ %>
            ●&nbsp;<span style="color: #ff6464;">Restricted <b> <%= moment.unix(user.ban_datetime).fromNow() %></b>.</span>
        <% } %>
    </div>
    <div class="card-body">
        <form action="/actions/restrict/<%= user.id %>" method="post">
            <div class="form-group">
                <label>User ID</label>
                <input type="text" class="form-control" name="accid" value="<%= user.id %>" readonly>
            </div>
            <div class="form-group">
                <label>Reason</label>
                <input type="text" class="form-control" name="reason" value="No Reason Provided">
            </div>
            <button class="btn btn-danger" type="submit" name=""><%= restricted ? "Unrestict" : "Restrict" %>!</button>
        </form>
    </div>
</div>